{
  "name": "motion-recipes",
  "private": true,
  "version": "0.1.0",
  "description": "A dependency-free library of production-ready web animation recipes built on the Web Animations API.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.11"
  }
}
